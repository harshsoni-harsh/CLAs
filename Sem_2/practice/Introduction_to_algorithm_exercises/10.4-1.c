// Draw the binary tree rooted at index 6 that is represented by the following attributes:

#include <stdio.h>
#include <stdlib.h>

typedef struct node
{
    int key;
    struct node* left;
    struct node* right;
}node;

